# Application configuration
# Copy this file to `.env` and fill the values before running the app.

# SERVER
PORT=8000
NODE_ENV=development
# comma separated origins allowed by CORS, e.g. http://localhost:3000,http://localhost:5173
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Database
# Local Mongo: mongodb://127.0.0.1:27017/forumiq
# Docker/container host: mongodb://mongo:27017/forumiq
DATABASE_URL=mongodb://127.0.0.1:27017/forumiq

# JWT
# Use a long random secret in production
ACCESS_TOKEN_SECRET=replace_with_a_long_random_secret
ACCESS_TOKEN_EXPIRES_IN=30d

# Google GenAI (optional)
# Provide your Gemini / Google Cloud API key if you want AI features enabled
GENAI_API_KEY=
GENAI_MODEL=gemini-2.5-flash

# (Optional) Additional environment vars you may want to set in production
# CORS_ORIGIN=https://your-frontend.example.com
# DATABASE_URL=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/forumiq
# ACCESS_TOKEN_SECRET=very_secure_secret_here
